CREATE TABLE purchases (
    id INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,
    username TEXT NOT NULL,
    quote_bought TEXT,
    shares NUMERIC,
    costs NUMERIC, 
    date DATETIME)
CREATE TABLE purchases (
    id INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,
    username TEXT NOT NULL,
    quote_bought TEXT,
    shares NUMERIC,
    costs NUMERIC, 
    date DATETIME);
DROP TABLE purchases
DROP TABLE purchases;
.schema
CREATE TABLE purchases (
    id INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,
    username TEXT NOT NULL,
    quote_bought TEXT,
    shares NUMERIC,
    costs NUMERIC, 
    date DATETIME);
.schema
.schema purchases
SELECT * FROM purchases
SELECT * FROM purchases;
SELECT * FROM users;
.schema
SELECT * FROM users
SELECT * FROM users;
SELECT * FROM purchases;
.schema
DROP TABLE purchases;
CREATE TABLE purchases (
    id INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,
    username TEXT NOT NULL,
    quote_bought TEXT,
    quote_name,
    shares NUMERIC,
    prices NUMERIC,
    total NUMERIC, 
    date DATETIME);
.schema
DROP TABLE purchases;
CREATE TABLE purchases (
    id INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,
    username TEXT NOT NULL,
    quote_bought TEXT,
    quote_name TEXT,
    shares NUMERIC,
    prices NUMERIC,
    total NUMERIC, 
    date DATETIME);
.schema
SELECT * FROM purchases;
.schema
SELECT * FROM purchases;
DELETE FROM purchases WHERE shares == 1 OR shares == 2;
SELECT * FROM purchases;
DELETE FROM purchases WHERE shares == 1 OR shares == 4;
SELECT * FROM purchases;
SELECT * FROM users
SELECT * FROM users;
SELECT * FROM purchases;
.schema
SELECT * FROM purchases;
.schema
SELECT * FROM purchases;
SELECT * FROM users
SELECT * FROM users;
DELETE FROM users *;
DELETE * FROM users;
DELETE FROM users;
SELECT * FROM users;
SELECT * FROM purchases;
DELETE FROM purchases;
SELECT * FROM purchases;
.schema
SELECT * FROM purchases;
DELETE FROM users;
DELETE FROM purchases;
.schema
DELETE FROM purchases;
DELETE FROM users;
SELECT * FROM users;
.schema
CREATE TABLE transactions(
            symbol TEXT,
            shares NUMERIC,
            price NUMERIC,
CREATE TABLE transactions(
            symbol TEXT,
            shares NUMERIC,
            price NUMERIC,
            date DATETIME);
.schema
SELECT * FROM transactions;

